// REPLACE ONLY THIS PART IN YOUR EXISTING page.tsx - inside LearningPage component

// 1. Add import at top
import { StudyPlan } from "./components/StudyPlan"

// 2. Replace your existing Today's Plan JSX with this:

const assignments = [
  { id: "a1", title: "Light - Reflection Worksheet", subject: "Science", chapter: "Light", dueLabel: "Today 5PM", status: "due_today" as const },
  { id: "a2", title: "Algebra Practice - Ch3", subject: "Math", chapter: "Algebra", dueLabel: "Tomorrow", status: "due_soon" as const },
]

const handleGoToChapter = (chapter: string, subject: string) => {
  setChapter(chapter)
  setSubject(subject)
  setTab("learn")
  window.scrollTo({ top: 400, behavior: "smooth" })
}

// In your return JSX, where you have Today's Plan:
<StudyPlan assignments={assignments} onGoToChapter={handleGoToChapter} onStartRecommended={() => setTab("learn")} />

// That's it - no other changes needed. Sidebar stays same, no new page.