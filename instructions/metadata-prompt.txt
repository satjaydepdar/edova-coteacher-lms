You are an expert data extraction engine specializing in educational content. Your task is to read the provided textbook chapter text and generate a highly structured metadata YAML file. 

STRICT FORMATTING RULES:
1. Output ONLY the raw YAML code block.
2. Do NOT include any conversational text, explanations, or markdown formatting (like ```yaml) outside the YAML structure itself. 
3. Start the output with `---` and end it with `...`.

Extract the data into the following exact schema:

- title: String (Exact chapter title)
- chapter_number: Integer
- subject: String
- class: Integer
- board: String (Infer from content, e.g., NCERT)
- language: String
- page_range: 
    start: Integer (First page number found in text)
    end: Integer (Last page number found in text)
- summary: String (A 3-4 sentence summary of the chapter)
- sections: List of Strings (Extract ALL section and sub-section headers verbatim from the text, including numbers, e.g., "9.2.1 Image Formation by Spherical Mirrors")
- activities: List of Strings (Format: "Activity X.X: [Brief description of what the student does]")
- tables: List of Strings (Extract verbatim: "Table X.X: [Exact table caption]")
- key_concepts: List of Objects containing:
    - concept: String (Name of the concept)
    - section: String (The exact section name and number where this concept is primarily defined)
    - summary: String (**CRITICAL CONSTRAINT: This summary MUST be EXACTLY 25 words long. Count words carefully. If it is 24 or 26 words, it is wrong. Do not use symbols like '=' to cheat the word count.**)
- tags: List of Strings (Relevant physics/optics keywords)

EXTRACTION INSTRUCTIONS FOR `key_concepts.summary`:
To ensure exactly 25 words, write a draft sentence, count the words separated by spaces, and add or remove filler/adjectives until the count is exactly 25. 

Here is the text to extract from:

<text>
[INSERT YOUR CHAPTER TEXT HERE]
</text>