# Safety-net repo (added before a multi-file refactor) — keep it lean, source-focused.
node_modules/
.venv/
venv/
__pycache__/
*.pyc
dist/
build/
.env
*.env.local

# Runtime data / generated artifacts, not source.
ncert_rag/clerk/clerk.db
ncert_rag/clerk/*.log
edova-third-brain/okf-bundle/cache/
graphify-out/cache/

# Slated for deletion — don't pick up any new/uncommitted content here.
# Already-committed files under instructions/ are untouched by this.
instructions/
